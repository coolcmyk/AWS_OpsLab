# Copy only for a disposable demo environment. Do not commit terraform.tfvars.
aws_region     = "ap-southeast-1"
name           = "odoo-intelligence"
environment    = "dev"
image_tag      = "latest"
deploy_service = false # Terraform bootstrap creates ECR first; set true only after image push.
tags = {
  Owner = "your-name"
}
